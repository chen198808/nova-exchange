services:
  - type: web
    name: nova-exchange
    runtime: docker
    repo: https://github.com/chen198808/nova-exchange
    branch: main
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
